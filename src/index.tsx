import React from "react";
import * as ReactDOM from "react-dom/client";

const container: Element = document.getElementById("root");
const root: ReactDOM.Root = ReactDOM.createRoot(container);

root.render(<h1>Hellopopup</h1>);
